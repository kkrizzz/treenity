const BulmaCard = render('dev', 'bulma-card');

add(() => {
  return (
    <Render id="explorer" name="layout">
      <div className="bu-container bu-is-max-desktop">
        <BulmaCard hea der="Blocks"></BulmaCard>
      </div>
    </Render>
  );
});
